name: Fast Repack KSU Boot Image (Official OEM Method)

on:
  workflow_dispatch:

jobs:
  repack-boot:
    runs-on: ubuntu-22.04
    steps:
      - name: Setup Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip python3-pip
          pip3 install gdown

      - name: Download Files
        run: |
          mkdir repack
          cd repack
          
          echo "Downloading Stock Boot (64MB)..."
          gdown "1FWNB6uFzPOsXR3o5APSP3k-Wg2hRYMdw" -O stock_boot.img
          
          echo "Downloading KSU Engine (25MB)..."
          gdown "1B4vYn0ZuPesPb40Q4ubs42_qrhxYyjuT" -O kernel

      - name: Get Official MagiskBoot Tool
        run: |
          cd repack
          
          echo "Downloading Official Magisk v27.0 APK..."
          wget -qO magisk.apk "https://github.com/topjohnwu/Magisk/releases/download/v27.0/Magisk-v27.0.apk"
          
          echo "Extracting tool directly from APK..."
          unzip -j magisk.apk "lib/x86_64/libmagiskboot.so" -d .
          mv libmagiskboot.so magiskboot
          chmod +x magiskboot

      - name: Repack Image
        run: |
          cd repack
          
          echo "Unpacking Stock Boot..."
          ./magiskboot unpack stock_boot.img
          
          echo "Repacking with new KSU Engine..."
          ./magiskboot repack stock_boot.img fastboot_ksu_boot.img

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: Poco-M4-Fastboot-KSU-Image
          path: repack/fastboot_ksu_boot.img

